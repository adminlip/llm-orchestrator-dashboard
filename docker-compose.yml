services:
  llm-orchestrator:
    build: .
    container_name: llm-orchestrator-dashboard
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
